export const sampleMarkdown = `# Markdown Annotator

Markdown 파일을 열면 이 영역에 렌더링됩니다.

## MVP 흐름

- 파일 열기
- Markdown 렌더링
- 텍스트 선택 기반 annotation 추가
- Agent용 Markdown 출력

> Annotation anchor는 DOM 위치가 아니라 stable node id와 offset에 붙이는 방향으로 설계합니다.
`;
